// src/components/nav/side/SalePeltNav.tsx
import { SalePeltNavClient } from './SalePeltNavClient';

export default function SalePeltNav() {
    return (
        <nav className="side-nav">
            <SalePeltNavClient />
        </nav>
    );
}
